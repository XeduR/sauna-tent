# Replays

Place `.StormReplay` files here for pipeline parsing. The pipeline scans this directory recursively.

Replay files are gitignored due to size. Use `collect_replays.py` to copy new replays from the local HotS documents folder automatically.
